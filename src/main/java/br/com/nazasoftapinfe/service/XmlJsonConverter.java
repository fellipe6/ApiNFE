package br.com.nazasoftapinfe.service;


public class XmlJsonConverter {
    /*
    public String convertXmlToJson(String xml) throws IOException {
        // Instância do XmlMapper para lidar com XML
        XmlMapper xmlMapper = new XmlMapper();
        // Converte o XML para um Map genérico
        Map<String, Object> map = xmlMapper.readValue(xml, Map.class);

        // Instância do ObjectMapper para gerar JSON
        ObjectMapper objectMapper = new ObjectMapper();
        return objectMapper.writeValueAsString(map);
    }

     */
}
